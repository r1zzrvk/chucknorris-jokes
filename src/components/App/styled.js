import { styled } from "styled-components";

const MainWrapper = styled.main`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 128px 162px 0px 162px;
`;

export const Styled = {
  MainWrapper,
};
